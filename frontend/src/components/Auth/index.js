import {connect} from "react-redux";
import Container from './container';

// add all the actiosn for:
// Log in
// Sign up
// Recover password
// Check username
// Check password
// Check email

export default connect()(Container);